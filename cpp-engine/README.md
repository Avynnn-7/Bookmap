# C++ Engine

This is the backend for my bookmap clone. 
It generates L2 orderbook data and sends it over to the server quickly.

## Requirements
- CMake
- C++17 compiler
- nlohmann_json

## Build

```bash
mkdir build
cd build
cmake ..
make
```

Then run the executable.
