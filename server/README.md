# Node.js Server

This server connects the C++ engine to the React frontend using WebSockets.

## How to use

1. Go to the `server` folder
2. Run `npm install`
3. Run `node server.js`

Make sure the C++ backend is running first so it can get the data!
